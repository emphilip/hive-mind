# adopt-graphifyy

Use graphifyy as the extraction library for code files: file discovery via collect_files(), symbol-aware chunking via extract(), and deterministic code graph (concepts + calls/imports/uses edges). Our paragraph chunker stays for text; our LLM extractor narrows to text-only.
